# infra/ci

**Status:** Not yet populated.

Reusable CI scripts (migration check, full compose-integration test, golden-query eval runner) will be added incrementally. See `.github/workflows/ci.yml` for what's wired up now.
