# @codeoracle/mcp-server

**Status:** Stub.

MCP tool implementations (`search_codebase`, `explain_file`, `find_decision` — I/O contracts in `@codeoracle/contracts/src/mcp.ts`, every result requires a citation field) are not built yet. Requires `@codeoracle/retrieval` to exist first.
