{
  "name": "@codeoracle/worker",
  "version": "0.0.0",
  "private": true,
  "description": "BullMQ consumers: chunk · embed · extract · reindex. STUB — see README.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@codeoracle/contracts": "workspace:*",
    "@codeoracle/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
