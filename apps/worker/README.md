# @codeoracle/worker

**Status:** Stub.

BullMQ consumers for `chunk_file`, `embed_chunks`, `extract_decisions`, and `incremental_reindex` (job contracts in `@codeoracle/contracts/src/jobs.ts`) are not wired up yet.
