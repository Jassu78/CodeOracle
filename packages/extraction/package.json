{
  "name": "@codeoracle/extraction",
  "version": "0.0.0",
  "private": true,
  "description": "Decision extraction pipeline, prompt templates, JSON contract. STUB — see README.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@codeoracle/contracts": "workspace:*",
    "@codeoracle/gateway": "workspace:*",
    "@codeoracle/config": "workspace:*",
    "@codeoracle/observability": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
