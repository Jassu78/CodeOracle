{
  "name": "@codeoracle/gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Universal OpenAI-compatible chat/embedding gateway with ordered provider failover. STUB — see README.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@codeoracle/contracts": "workspace:*",
    "@codeoracle/config": "workspace:*",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "ioredis-mock": "^8.13.1",
    "tsup": "^8.3.5",
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
